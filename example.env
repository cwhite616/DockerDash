# Copy this file to .env and edit the values

# Optional: auth mode. "password" (default) requires the PASSWORD below.
# "none" disables login entirely - use only for local/trusted environments.
AUTH=password

# Required when AUTH=password: dashboard login password
PASSWORD=changeme

# Required: any random secret string for JWT signing
JWT_SECRET=please-change-me

# Optional: backend listen port (compose maps 8080:8080)
PORT=8080

# Optional: UI theme. One of the built-in names below, or a path to a custom
# CSS file, resolved relative to this .env file's directory
# (e.g. THEME=./my-theme.css picks up ./my-theme.css next to .env).
# Built-in: basic-dark (default), basic-light, nord, solarized, solarized-dark,
# ubuntu, cga, terminal
THEME=basic-dark
